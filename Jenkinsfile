pipeline {
     stages{
          stage("Run tests") {
              steps {
                  sh '''
                    echo "Hello world!"
                  '''
              }
          }
     }
}
