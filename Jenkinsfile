pipeline {
     agent any
     stages{
          stage("Run tests") {
              steps {
                  sh '''
                    echo "Hello world!"
                  '''
              }
          }
     }
}
